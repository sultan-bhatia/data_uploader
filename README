data_uploader

data_uploader is an intuitive user interface for mapping csv incoming data to multiple database tables. 

The code can easily be extracted and merged into an existing application.

Visit www.rubysultan.com for a demo.
 
